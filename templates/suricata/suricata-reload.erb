#!/bin/bash

#
# /usr/sbin/suricata-reload
#
# Generated by Chef for <%= node[:fqdn] %>
#


suricata_process_id=$(pidof suricata)
if [[ -z $suricata_process_id ]]; then
    /usr/bin/systemctl start suricata
else
	kill -usr2 $suricata_process_id
fi

#if [ ! -f /nsm/suricata/suricata_initial_update ]; then
#    kill -usr2 $(pidof suricata)
#else
#	touch /nsm/suricata/suricata_initial_update
#fi

