#
# /etc/nsm/rules/local.rules
#
# Generated by Chef for <%= node[:fqdn] %>
#

##########################
# Global
##########################

<% @global_sigs.each do |msg, sig| %>
	<% if sig %>
<%= "\# #{msg}" %>
<%= "#{sig}" %>

	<% end %>
<% end %>

##########################
# Sensor Region
# <%= node[:nsm][:sensor_region] %>
##########################

<% @sensor_region_sigs.each do |msg, sig| %>
	<% if sig %>
<%= "\# #{msg}" %>
<%= "#{sig}" %>

	<% end %>
<% end %>


##########################
# Sensor Group 
# <%= node[:nsm][:sensor_group] %>
##########################

<% @sensor_group_sigs.each do |msg, sig| %>
	<% if sig %>
<%= "\# #{msg}" %>
<%= "#{sig}" %>

	<% end %>
<% end %>

##########################
# Host Chef FQDN
# <%= node[:fqdn] %>
##########################

<% @host_sigs.each do |msg, sig| %>
	<% if sig %>
<%= "\# #{msg}" %>
<%= "#{sig}" %>

	<% end %>
<% end %>

