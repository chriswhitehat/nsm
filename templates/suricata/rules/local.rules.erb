#
# /etc/nsm/rules/<%= @sniff[:sensorname] %>/local.rules
#
# Generated by Chef for <%= node[:fqdn] %>
#

##########################
# Global
##########################

<% @global_sigs.each do |msg, sig| %>
	<% if sig %>
<%= "\# #{msg}" %>
<%= "#{sig}" %>

	<% end %>
<% end %>

##########################
# Regional
##########################

<% @regional_sigs.each do |msg, sig| %>
	<% if sig %>
<%= "\# #{msg}" %>
<%= "#{sig}" %>

	<% end %>
<% end %>


##########################
# Sensor Group 
# <%= node[:seconion][:sensor][:sensor_group] %>
##########################

<% @sensor_group_sigs.each do |msg, sig| %>
	<% if sig %>
<%= "\# #{msg}" %>
<%= "#{sig}" %>

	<% end %>
<% end %>

##########################
# Host
##########################

<% @host_sigs.each do |msg, sig| %>
	<% if sig %>
<%= "\# #{msg}" %>
<%= "#{sig}" %>

	<% end %>
<% end %>


##########################
# Sensor 
##########################

<% @sensor_sigs.each do |msg, sig| %>
	<% if sig %>
<%= "\# #{msg}" %>
<%= "#{sig}" %>

	<% end %>
<% end %>
