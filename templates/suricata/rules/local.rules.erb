#
# /etc/nsm/rules/<%= @sniff[:sensorname] %>/local.rules
#
# Generated by Chef for <%= node[:fqdn] %>
#

##########################
# Global
##########################

<% @global_sigs.each do |msg, sig| %>
	<% if sig %>
<%= "\# #{msg}" %>
<%= "#{sig}" %>

	<% end %>
<% end %>

##########################
# Sensor Region
# <%= @sniff[:sensor_region] %>
##########################

<% @sensor_region_sigs.each do |msg, sig| %>
	<% if sig %>
<%= "\# #{msg}" %>
<%= "#{sig}" %>

	<% end %>
<% end %>


##########################
# Sensor Group 
# <%= @sniff[:sensor_group] %>
##########################

<% @sensor_group_sigs.each do |msg, sig| %>
	<% if sig %>
<%= "\# #{msg}" %>
<%= "#{sig}" %>

	<% end %>
<% end %>

##########################
# Host Chef FQDN
# <%= node[:fqdn] %>
##########################

<% @host_sigs.each do |msg, sig| %>
	<% if sig %>
<%= "\# #{msg}" %>
<%= "#{sig}" %>

	<% end %>
<% end %>


##########################
# Sensor 
# <%= @sniff[:sensorname] %>
##########################

<% @sensor_sigs.each do |msg, sig| %>
	<% if sig %>
<%= "\# #{msg}" %>
<%= "#{sig}" %>

	<% end %>
<% end %>
